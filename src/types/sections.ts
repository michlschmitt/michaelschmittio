// section types
export type PortfolioPreviewSectionImagePosition = 'left' | 'right';

export enum PortfolioPreviewSectionImagePositionEnum {
  left = 'left',
  right = 'right',
}

export type PortfolioPreviewSectionColor = 'gradient' | 'black';

export enum PortfolioPreviewSectionColorEnum {
  gradient = 'gradient',
  black = 'black',
}
